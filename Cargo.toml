[package]
name = "dutch-gen"
version = "0.1.1"
edition = "2024"

[dependencies]
flate2 = "1.0"
rand = "0.10.1"
rand_chacha = "0.10.0"

[lints.clippy]
pedantic = "forbid"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
